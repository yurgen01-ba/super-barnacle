# Commit 172 — Job → Extraction Integration

Creates Extraction and Artifact records when meeting processing completes.

```powershell
git add jobs/extraction_artifact_integration.py jobs/extraction_tasks.py README_COMMIT_172_JOB_TO_EXTRACTION_INTEGRATION.md
git commit -m "Commit 172 - Connect meeting jobs to Extraction artifacts"
```
