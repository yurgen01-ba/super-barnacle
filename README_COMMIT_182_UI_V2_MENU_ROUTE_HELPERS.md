# Commit 182 — UI v2 Menu Route Helpers

Uses explicit `open_artifacts()` state helper from menu.

```powershell
git add ui_v2/layout/menu.py README_COMMIT_182_UI_V2_MENU_ROUTE_HELPERS.md
git commit -m "Commit 182 - Use UI v2 route helpers in menu"
```
