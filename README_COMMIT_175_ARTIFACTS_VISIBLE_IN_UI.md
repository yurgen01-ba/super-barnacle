# Commit 175 — Artifacts Visible in UI

Keeps the just-created extraction selected when user opens Knowledge Artifacts.

```powershell
git add ui/artifacts/artifact_framework.py README_COMMIT_175_ARTIFACTS_VISIBLE_IN_UI.md
git commit -m "Commit 175 - Make generated artifacts visible in UI"
```
