# Commit 191.1 — Fix WhisperX dependency conflict

Removes direct dependency pins that conflict with WhisperX's own dependency graph.

## Git commands

```powershell
git add requirements-whisperx.txt docs/WHISPERX_INSTALL_COMPATIBILITY.md README_COMMIT_191_1_FIX_WHISPERX_DEPENDENCY_CONFLICT.md
git commit -m "Commit 191.1 - Fix WhisperX dependency conflict"
```
