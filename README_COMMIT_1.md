# Project Brain — Commit 1

This commit introduces a cleaner modular architecture while preserving the current MVP functionality.

## Run

```powershell
python -m streamlit run app.py
```

## After testing

```powershell
git add .
git commit -m "Commit 1 - Foundation UI split and progress engine"
```

## Test checklist

- App starts.
- Meetings tab opens.
- Slack tab processes pasted messages.
- Jira text processing works.
- Jira PDF upload works.
- Memory tab shows saved items.
- Report tab generates executive summary.
