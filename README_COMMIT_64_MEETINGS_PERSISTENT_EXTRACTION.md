# Commit 64 — Meetings Persistent Extraction

```powershell
git add ui/meetings.py README_COMMIT_64_MEETINGS_PERSISTENT_EXTRACTION.md
git commit -m "Commit 64 - Run meeting extraction as persistent job"
```
