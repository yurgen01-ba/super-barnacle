# Commit 181 — UI v2 State Cleanup

Adds explicit helpers for opening Knowledge Artifacts and selected extraction state.

```powershell
git add ui_v2/state.py README_COMMIT_181_UI_V2_STATE_CLEANUP.md
git commit -m "Commit 181 - Add UI v2 artifact navigation state helpers"
```
