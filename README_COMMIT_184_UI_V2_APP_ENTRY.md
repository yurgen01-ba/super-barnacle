# Commit 184 — UI v2 App Entry

Makes UI v2 the single app entry point.

```powershell
git add app.py README_COMMIT_184_UI_V2_APP_ENTRY.md
git commit -m "Commit 184 - Make UI v2 the default app entry"
```
